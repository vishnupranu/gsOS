{
  "name": "@monorepo/docs",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "shiki": "^1.0.0"
  }
}